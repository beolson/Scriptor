---
platform: ubuntu-24.04-x64
title: Install curl
---

Installs the `curl` command-line tool for transferring data with URLs.

## What it does

Updates the package list and installs the latest version of `curl` from the Ubuntu 24.04 repositories.

## Requirements

- Ubuntu 24.04
- sudo access
