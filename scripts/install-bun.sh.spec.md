## Overview

Installs the Bun JavaScript runtime using the official curl-based installer
from bun.sh.

## Steps

1. Run the official Bun install script
2. Add `~/.bun/bin` to `PATH` in `~/.bashrc`

## Verification

```bash
bun --version
```
