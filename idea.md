We are going to work together to refine and define a process.  I have the start of that in @Process.md .  We will not be implementing code, just gathering and
  definign this process fia Q&A elicitation session.  Only ask me one question at a time and use the ask user question tool.

  The process.md document has the current process definition.  We will be updating it and modifying with the feedback below the feedback marker

You will also compile a list of outstanding questions you have about the process. Prioritize those questions by impact and importance and ask the highest one. After each answer, you will update the requirements document with any discovered or updated requirements and review your list of questions. Update that list of questions after every question.

**Feedback**

This process will be orchistrated by an agent.  It should start by reading the current_state.md, to set the phase if applicable.  If there is a current activity type, it should ask the user if they would like to resume.  Also, that activity type should also point to the current draft document for that activity.  

activitys are also explicitly decalred complete by the user.  

At some point, a phase will be marked ready for implementation.  To do this, we whsould review that all child documents in the phase are marked as complete.  If not, we should ask the user if they woul dlike to continue working one of them, or not


